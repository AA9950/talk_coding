#  Create a set called `fruits` with the following items
EX: fruits = {'apple', 'banana', 'cherry'}
fruits = print(fruits)
use to access a set with it elements

# print (fruits)
to show the result of the output to print the sets elements